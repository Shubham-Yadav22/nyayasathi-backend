services:
  - type: web
    name: nyayasathi-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000 --workers 1 --limit-max-requests 20
    envVars:
      - key: GROQ_API_KEY
        sync: false
    plan: free
