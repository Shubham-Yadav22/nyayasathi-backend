# Core FastAPI dependencies (using versions with pre-built wheels)
fastapi==0.103.0
uvicorn==0.23.2

# Lightweight vector DB and embedding
chromadb==0.4.15
sentence-transformers==2.2.2

# HTTP requests
requests==2.31.0

# Use older pydantic version with pre-built wheels
pydantic==1.10.12